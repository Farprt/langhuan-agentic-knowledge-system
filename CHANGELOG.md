# Changelog

本文件只记录公开仓库中可以由代码和测试验证的变化。

## 0.2.0 - 2026-08-11

### 新增

- 增加不读取正文的结构索引，可按路径、标题、别名、分类和显式链接定位文件。
- 增加面向单个目标的任务上下文包，返回应读取文件、处理方式和必须执行的检查。
- 增加稳定笔记 ID、可选材料处理记录以及结构回归与 Agent 证据评测命令。
- 增加统一的本地运行记录，使任务、检索和验证步骤能够共享关联 ID。
- 增加 AgentLoop 与 Langfuse 脱敏预览和显式发送接口。

### 改进

- 检查规则组改由配置声明；未声明值会在配置加载或运行时明确失败。
- README 使用普通中文解释公开接口，同时保留代码名便于检索。
- 补齐公开代码、示例配置、架构文档和测试之间的一致性检查。

### 隐私边界

- 本地运行记录默认不包含提示词、回复、查询正文或文件内容。
- 云端导出默认只预览；只有显式使用 `--send` 才联网。
- 凭据必须通过环境变量传入，不进入项目配置和 Git；外部密钥管理工具可以在启动前注入这些变量。

## 0.1.0 - 2026-07-23

- 首次公开本地 Markdown 解析、增量索引、混合检索、离线演示和交互式项目总览。
